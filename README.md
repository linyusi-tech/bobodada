# bobodada.cn

Static birthday site deployed with GitHub Pages.

Update flow:

```sh
git add index.html assets images CNAME .nojekyll .gitignore README.md LICENSE.txt
git commit -m "Update birthday site"
git push
```
