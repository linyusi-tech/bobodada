window.BOBO_BIRTHDAY_CONFIG = window.BOBO_BIRTHDAY_CONFIG || {
	// Leave blank when the site and API are deployed together, for example on Vercel.
	// If the static site stays on GitHub Pages, set this to the deployed API URL:
	// wishApiUrl: 'https://your-api.example.com/api/wishes'
	wishApiUrl: ''
};
